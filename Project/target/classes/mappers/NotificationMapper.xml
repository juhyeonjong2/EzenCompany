<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ezen.ezencompany.mapper.notificationMapper">
	
	<select id="getNoti" parameterType="int" resultType="notificationVO">
		SELECT code, targetname, nno FROM notification where mno = #{mno} and nconfirm = 1 order by nno DESC limit 0,3;
	</select>
	
	<update id="checkNoti" parameterType="int">
		update notification set nconfirm = 2 where nno = #{nno};
	</update>
</mapper>
	